package model;

public class Some2 {

	public Some2(String commandWithoutSemicolon) {
		final var a = command.split();
		asd = new Some2[a.length];
		for (int i = 0; i < a.length; i++) {
			asd[i] = new Some2(a[i]);
		}
	}

}
